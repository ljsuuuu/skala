server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # 🚀 핵심: Vue 라우터(History 모드)를 위한 설정
        try_files $uri $uri/ /index.html;
    }
}